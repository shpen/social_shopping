<li id="question-<%= question.id %>">
  <span class="title"><%= link_to question.title, '#' %></span>
  <span class="content"><%= question.description %></span>
  <span class="timestamp">Posted <%= time_ago_in_words(question.created_at) %> ago.</span>
</li>